<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.UNIAOS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Neutral Key Color -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnBackground</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your theme here. -->
        <item name="colorControlActivated">@color/Lightblue_300</item>
        <item name="android:textColorHighlight">@color/Lightblue_200</item>
    </style>

    <style name="Theme.UNIAOS" parent="Base.Theme.UNIAOS" />

    <!-- Custom Stutus bar color -->
    <style name="Theme.UNIAOS.LightBlueStatusBar" parent="Base.Theme.UNIAOS">
        <item name="android:statusBarColor">@color/Lightblue_500</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.UNIAOS.GrayStatusBar" parent="Base.Theme.UNIAOS">
        <item name="android:statusBarColor">@color/Gray_100</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="DefaultNumberPickerTheme" parent="Base.Theme.UNIAOS">
        <item name="android:textSize">40sp</item>
        <item name="android:textColor">@color/Gray_600</item>
        <item name="fontFamily">@font/pretendard</item>
    </style>

</resources>